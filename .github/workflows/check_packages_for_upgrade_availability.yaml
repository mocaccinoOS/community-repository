name: Check packages for upgrade availability

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: [self-hosted, x64]
    steps:
      - name: Check packages for upgrade availability
        run: |
                ./scripts/run.sh up.sh
