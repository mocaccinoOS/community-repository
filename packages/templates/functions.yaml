{{ define "requires" }} 

{{ $ := index . 0 }}

{{ $items := list }}

{{ with index . 1 }}

{{ $items = printf "%s-%s" (.Values.category | replace "\"" "") (.Values.name | replace "\"" "") | append $items }}

{{ if .Values.requires }}
{{ range .Values.requires }}
{{ $items = printf "%s-%s" (.category | replace "\"" "") (.name | replace "\"" "") | append $items }}
{{ end }}
{{ end }}

{{ end }}

{{ join " " $items }}

{{ end }}
